document.addEventListener('DOMContentLoaded', function() {
    // Futuramente, aqui entrará o código para:
    // - Filtrar a tabela de modelos de acordo com o grupo selecionado
    // - Abrir o modal para "Gerenciar Grupos"
    // - Controlar os botões de ação (usar modelo, editar, excluir)
});